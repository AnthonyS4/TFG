Comando de copia para el debugger:
cp /home/anthony/go/scr/wireshark/config.yml /home/anthony/go/bin/config.yml

Comando de ejecucion del debugger:
./dlv debug /home/anthony/go/scr/wireshark/wireshark.go

Existe una clase sync.WaitGroup que actua como barrera para que la gorutina principal
  espere a las demas gorutinas, hay que usar el metodo Done() para notificar que una 
  gorutina ha terminado

Ejecutar los comandos usando "bash" y "-c" como primeros argumentos de la llama a exec.Command()

Aqui un ejemplo de readers:
//inputpipe, error := commandExecutionTshark.StdinPipe()

	//	var buffer io.Reader
	//	buffer = strings.NewReader("")
	//	commandExecutionTshark.Stdin = buffer
	//	commandExecutionTshark.Stdin.Read([]byte("790321"))
	/*for i := 0; i < len((*commandExecutionTshark).Args); i++ {
		fmt.Println((*commandExecutionTshark).Args[i])
	}*/
